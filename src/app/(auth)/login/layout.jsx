
export const metadata = {
    title: "Login Page",
    description: "Blue_Sky - Login Page ",
};


const LoginLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
}

export default LoginLayout;

export const metadata = {
    title: "Update Driver",
    description: "Blue_sky - Rent the perfect motorhome or campervan in America",
};


const UpdateDriverLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
}

export default UpdateDriverLayout;
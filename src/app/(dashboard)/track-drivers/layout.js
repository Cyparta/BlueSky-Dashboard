export const metadata = {
    title: "Track Drivers - Blue_Sky",
    description: "Blue_Sky - Rent the perfect motorhome or campervan in America",
};

const TrackDriversLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
};

export default TrackDriversLayout;
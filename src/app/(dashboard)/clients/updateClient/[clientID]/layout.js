export const metadata = {
    title: "Update CLIENTS",
    description: "Blue_sky - Rent the perfect motorhome or campervan in Europe",
};


const UpdateClientLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
}

export default UpdateClientLayout;
export const metadata = {
    title: "Update Pet Car",
    description: "Blue_sky - Rent the perfect motorhome or campervan in America",
};


const UpdatePetCarLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
}

export default UpdatePetCarLayout;
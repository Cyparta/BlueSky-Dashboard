
export const metadata =  {
    title: "Update Group PAGE",
    description: "Blue_sky - Rent the perfect motorhome or campervan in America",
};


const UpdateGroupLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
}

export default UpdateGroupLayout;